export const metadata = {
  title: 'Seed Database - eNumismatica',
  description: 'Database seeding tool',
}

export default function SeedDbLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}

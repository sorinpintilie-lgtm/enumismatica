[build]
  # Directory to publish
  publish = ".next"
  
  # Build command - reinstall lightningcss to get native binaries, then build with webpack
  command = "cd /opt/build/repo && npm install && cd web && npm run build -- --webpack"
  
  # Environment variables for build time
  [build.environment]
    NODE_VERSION = "20"

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Next.js specific configuration
[build.processing]
  skip_processing = false

[build.processing.css]
  bundle = true
  minify = true

[build.processing.js]
  bundle = true
  minify = true

[build.processing.html]
  pretty_urls = true